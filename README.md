# Language-Neural-Network
Accent classifiers that takes in audio files to categorize the English speakers as Native, FirstGen (Asian Americans), or Immigrants. It extracts MFCC features from audio samples and trains a neural network based on the given audio samples (from each class) to make a prediction. It uses an interactive tkinter interface that display graphs and more
